# Execution Plans

Use `active/` for in-progress plans and `completed/` for validated finished plans.

Create a new plan when a task:

- touches multiple files or subsystems
- has non-obvious sequencing
- needs decision tracking
- cannot be completed safely from short-term chat context alone

Recommended filename:

`YYYY-MM-DD-short-task-name.md`
